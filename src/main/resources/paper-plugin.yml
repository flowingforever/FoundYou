name: FoundYou
version: '${version}'

main: pro.fazeclan.river.foundyou.FoundYou
bootstrapper: pro.fazeclan.river.foundyou.FYBootstrap
loader: pro.fazeclan.river.foundyou.FYLoader
api-version: '26.2'
load: STARTUP

authors: [ riversflowing ]

dependencies:
  server:
    Jarona:
      load: BEFORE
      required: true
    NBTAPI:
      load: BEFORE
      required: true
      join-classpath: true